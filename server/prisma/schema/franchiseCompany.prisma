model FranchiseCompany {
    id                  Int      @id @default(autoincrement())
    name                String
    brandPictureAddress String
    branches            Place[]
    createdAt           DateTime @default(now())
    @@map("franchiseCompanies")
}
